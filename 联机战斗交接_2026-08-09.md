# 联机战斗交接文档（2026-08-09）

## 当前状态

大厅 → 战斗全流程已通（登录/邀请/进房/开始/出生点/快照/输入/纠偏），**Boss 同步（M11）骨架已搭，但表现仍有多处问题未解决**。

## 已完成（可运行）

- 大厅→战斗：`JoinRoom` → `BattleSessionState` → `RoomStart` 广播 → 双方进 Main → 房主自动起 7778、客人自动连
- 服务器权威出生点：槽位 x+2，`JoinAck` 下发，客户端直接生成（非 host 不走 PlayerSpawnPoint）
- 玩家同步：输入上报（世界方向）、`MoveCanceled` 边沿锁存、快照 30Hz、幽灵延迟插值、攻击/受击/闪避期间跳过纠偏、入场缓冲 0.5s
- 单机模拟 Bot（`SimBattleBot`，`AutoCreate` 已注释，需要时取消注释）
- M11 骨架：Boss 进快照（位置/AnimHash/HP）、客户端 Boss 幽灵化、`BossGhostBrain` 受击表现、Boss 血条/死亡事件

## 未解决 / 待办（重点）

1. **Boss 同步表现**（用户反馈“还是都有问题”）：
   - Boss 动画断续：快照 AnimHash 频繁切换（已加 3 帧防抖，待验证）
   - Boss 位移闪现/飞很远：已禁跳变瞬移；但主机侧行为树“冲刺权重 100 / 移动参数空”导致 Boss 频繁冲刺；对峙因黑板缺键（已补 4 键，待验证）
   - 客户端 Boss 表现与主机不完全同步
2. **打击特效同步**：远端受击特效事件未做完整
3. **M11 伤害闭环**：客户端攻击→主机判定→Boss 血条（事件已搭，待验证）
4. **M12 结算**：Boss 死亡 → `EnemyDied` → WinView 已接，待验证
5. **HUD 打包问题**：GamePanel 打包后加载失败（AB 已切成功但 Open 失败，需在 Unity 确认 bundle 内容/路径）
6. **大厅服务器需重启**加载 RoomStart 新代码（`LobbyServer/bin` 被运行进程锁定）

## 关键文件

- 战斗会话：`Assets/Scripts/Net/Battle/*`（BattleServer/BattleClient/BattleHostRuntime/BattleClientRuntime/BattleGhostInterpolator/SimBattleBot/BossGhostBrain/BattleSessionState/BattleDevKit...）
- 大厅：`Assets/Scripts/Net/Lobby*`、`LobbyServer/`
- 行为树：`Assets/Scripts/AI/BehaviorTree/NewBehaviorTree.asset`、`Assets/Scripts/AI/Config/AI/MonsterBlackboard.asset`
- 教学：`战斗会话教学_01_状态同步与帧同步.md`、`联机与热更开发计划.md`

## 测试方式

- 单机模拟：`SimBattleBot.AutoCreate` 取消注释 + 大厅服务器 `cd LobbyServer && dotnet run`
- 双端：打包 exe（`HotUpdateCDN` 放 exe 旁），编辑器房主 + exe 客人
- 日志：Console 筛 `[BattleFlow]`（流程）/ `[SyncDebug]`（偏差）；Bot 文件 `SyncBotLog.txt`

## 下一步建议

1. Unity 里确认 Boss Animator：对峙 2D 树、攻击/冲刺动画 root motion 位移、状态名
2. 调行为树：冲刺权重/移动参数，让 Boss 行为正常（决定快照动画是否稳定）
3. 验证 Boss 快照 / 血条 / 结算闭环
