# 联机与热更开发计划（秋招 Demo）

> 面向开发者与 AI agent。目标：PC 局域网联机 + 热更，**手搓网络框架**（不用 Mirror/Netcode 等高层框架），边做边学。
> 本文档结合**项目现状**与**制作预期阶段**编写，按"先单机地基 → 联机基础 → 同步框架 → 热更"推进。

---

## 一、目标与已确认决策

- **平台**：PC 局域网，本机调试（一台电脑多进程模拟多客户端）。
- **手搓网络**：不用现成高层框架；底层用 `System.Net.Sockets` TCP（`TcpListener`/`TcpClient`），消息用**长度前缀帧 + Protobuf.net** 序列化。
- **同步模型**：**主机权威状态同步**（快照 + 插值，不做 lockstep）。
- **拓扑**：邀请者 = 主机客户端 = 服务器（Listen Server）；其余为纯客户端。
- **数据库/好友**：本地服务器 + 本地 JSON 文件（演示用）。
- **登录唯一名**：创建时读取本地 JSON 文件名，重名不可创建。
- **热更**：YooAsset 自带版本管理，AB 资源放**本地文件夹**模拟 CDN。
- **场景**：六分街/战斗场景由开发者制作，代码层（加载/流程/联机/同步/热更）由 agent 负责。
- **加载**：登录后进入加载界面，加载过程中启动各模块 + 异步加载场景与预制体资源（角色、UI 面板、六分街资源等）；AB 资源版本不同则走热更。

---

## 二、项目现状盘点

### 已有
| 能力 | 现状 |
| --- | --- |
| 启动/模块 | `GameEntry` + `GameModules.Init()`（Resource → GameState → UI），`GameModule<T>`/`Singleton<T>` 单例体系 |
| 资源加载 | `ResourceManager`（Resources.LoadAsync + 缓存，UniTask）——**只有单场景、无 AB** |
| UI | `UIManager`/`UIView`/`UIObjectPool`，主 HUD `GamePanel`、`DamageText` 伤害跳字 |
| 事件 | `EventBus` + `GameEvents`（已预留 `network.player.connected/disconnected`） |
| 角色 | 双状态机（Locomotion + Action）、`PlayerController`、`CharacterBaseAttribute`、动画事件桥接 |
| 战斗 | `AttackHitHelper` 统一命中/暴击、`IDamageable`、`VFXPool` 特效池、时停/震屏/后处理 |
| 怪物 | 自研行为树（SO + Runner + 可视化编辑器）、`BossBrain`/`BossMotor`、死亡分支 |
| 插件 | UniTask（本地插件，含 YooAsset 适配脚本）、DOTween、Cinemachine、InputSystem、TMP、URP 14 |

### 缺失（本计划要补）
- **多场景**：目前只有 `Main.unity`，无 Boot/Login/六分街/战斗场景拆分。
- **登录/好友/数据库**：无任何账号、好友、数据持久化代码。
- **网络**：无传输、协议、会话、同步代码（完全从零）。
- **Protobuf.net**：未安装。
- **YooAsset**：本体未安装（只有 UniTask.YooAsset 适配脚本）。
- **AB 分包**：未做资源拆分与构建管线。

---

## 三、整体游戏流程

```
登录（唯一玩家名）
  → 加载界面（启动各模块；异步加载场景/角色/UI/六分街资源；AB 版本不同 → 热更）
  → 六分街（普通场景）
  → Boss 入口 → 战前选择界面（4 格：有玩家=角色立绘+用户名，空位=加号；下方"进入游戏"按钮，人数≥1 可开）
  → 点加号 → 好友列表浮层（在线好友可邀请；按用户名检索；可添加好友）
  → 邀请好友进入 → 跳转战斗场景
  → 战斗 → 结算 → 回六分街
```

---

## 四、分 Scene 方案

### 概念
- **场景（.unity）= 实例容器**：放灯光、相机、环境、角色实例、UI 面板实例。
- **预制体（prefab）= 资产**：不属于场景；场景里放 prefab 的实例。

### 场景划分（建议）
| 场景 | 内容 | 职责 |
| --- | --- | --- |
| `Boot` | `GameEntry`（DontDestroyOnLoad）、常驻 UI 根、全局管理器 | 启动 + 常驻系统，永不卸载 |
| `Login` | 登录 UI（唯一名）、加载 UI | 登录 → 过场 |
| `六分街` | 街道环境、玩家出生点、Boss 入口触发器、VCam | 主城 |
| `Boss战` | 现有 `Main.unity` 改造：Boss、玩家、战斗 Canvas、VCam | 战斗 |

> `BossLobby`（战前选择）建议做成**常驻 UI 面板**浮在六分街/战斗前，不单独建场景，少一条场景链路。

### 常驻 vs 场景内容
- `GameEntry` 挂 `DontDestroyOnLoad`：管理器、UI 根、之后还有**网络层**常驻。
- 场景切换用 `SceneManager.LoadSceneAsync(非叠加)`：新场景加载完，旧场景物体销毁，DontDestroyOnLoad 保留。
- 常驻：系统模块、UI 根、网络连接、玩家数据；按需加载：场景资源、角色 prefab、UI 面板。
- **角色不复制进每个场景**：玩家做成 prefab 资产，场景里只放"出生点"空物体，进场景后由资源管理器实例化。

### 操作步骤（开发者侧）
1. 现有 `Main.unity` 另存为 `Boss` 场景，删掉登录/大厅相关内容，只留战斗；
2. 新建 `Boot`（放 GameEntry）、`Login`、`六分街`（环境 + 出生点 + Boss 入口触发器）；
3. 全部场景加进 `Build Settings → Scenes in Build`（不加打包后加载报错）；
4. 玩家/UI 面板做成 prefab 放 Resources（后续 YooAsset），代码加载实例化；
5. 常驻 Canvas 由 Boot/UIManager 创建，登录/加载/战前选择面板挂其下。

---

## 五、分阶段模块计划

### 阶段 0：单机地基（联机前必须跑通）

| 模块 | 目标 | 关键产出 | 验收 |
| --- | --- | --- | --- |
| **M0 常驻启动架构** | Boot 场景 + 常驻系统 | 改造 `GameModules`/`SceneLoader`；常驻 UI 根 | 任意场景切换后管理器/UI 根不丢 |
| **M1 资源加载抽象** | 资源层留接口 | `IAssetProvider`（先 Resources 实现，预留 YooAsset）；异步加载 + 进度条 | 异步加载可跑、进度真实 |
| **M2 流程状态机** | 流程驱动场景 | 扩展 `GameStateManager`：Login → Loading → 六分街 → BossLobby → Boss 战 | 流程状态切换驱动场景加载 |
| **M3 登录 + 加载界面** | 登录/过场 | 唯一名校验（本地 JSON，重名不可创建）；`LocalPlayerData` 存取 | 输名字 → 过场 → 进六分街 |
| **M4 六分街 + 战前选择** | 单机闭环 | 六分街场景 + 出生点机制；战前选择 4 格 UI（常驻面板）；本地假玩家可开战 | 单人可完整走到战斗场景 |

### 阶段 1：联机基础

| 模块 | 目标 | 关键产出 | 验收 |
| --- | --- | --- | --- |
| **M5 手搓传输层** | 字节级收发 | `ITransport` 接口 + TCP 实现（TcpListener/TcpClient、连接/断线管理、长度前缀帧） | 两进程互发消息、断线可感知 |
| **M6 协议 + 序列化** | 消息编解码 | 安装 Protobuf.net；消息基类（msgId + body）；编解码 + 按 msgId 路由 | 消息可编解码、可路由 |
| **M7 会话/房间层** | 组队进房 | Listen Server 模型：主机=服务器+客户端，客户端连主机；房间状态、加入/离开、名字同步到 4 格 | 第二客户端加入后 4 格界面同步 |
| **M8 好友系统（本地 JSON）** | 好友数据 | `FriendDatabase`（本地 JSON：玩家表 + 好友关系）；按名检索、在线状态、邀请消息 | 本地检索 + 邀请"在线"好友 |

### 阶段 2：同步框架（手搓核心）

| 模块 | 目标 | 关键产出 | 验收 |
| --- | --- | --- | --- |
| **M9 输入抽象** | 解耦本地/远端输入 | `IInputProvider`（本地输入 / 远端指令队列），改造 `PlayerController` 读取源 | 单机行为不变，遥控可驱动角色 |
| **M10 主机权威状态同步** | 角色同步 | 移动/朝向快照（定频下发 + 插值）；攻击/受击/伤害/死亡事件同步；主机校验 HP | 双端看到对方平滑移动、伤害一致 |
| **M11 怪物同步** | 怪物同步 | 怪物主机驱动，广播位置/血量/死亡 | 客户端看到的 Boss 与主机一致 |
| **M12 战斗边界** | 结算一致 | 伤害数字、结算事件（PlayerDied/EnemyDied）跨端一致 | 4 人打完 Boss 各端结算一致 |

### 阶段 3：热更

| 模块 | 目标 | 关键产出 | 验收 |
| --- | --- | --- | --- |
| **M13 AB 分包** | 资源拆分 | 按场景/角色/UI/战斗分 AB + 构建管线 | 可打出分包 AB |
| **M14 YooAsset 集成** | 热更链路 | 安装 YooAsset + 接已有 UniTask.YooAsset 适配；HostPlayMode，下载源指向本地文件夹；版本对比 → 更新 | 改一版资源 → 客户端拉新 |
| **M15 资源提供者切换** | 全流程 AB | `IAssetProvider` 换 YooAsset 实现，Resources 兜底 | 全流程走 AB 加载 |

---

## 六、建议顺序与里程碑

```
M0 → M1 → M2 → M3 → M4 → M5 → M6 → M7 → M9 → M10 → M11 → M12 → M8 → M13 → M14 → M15
```

（M8 好友系统可与 M7 并行/后置，不影响主链路）

**里程碑**
- M4 完成：单机全流程可玩（登录 → 六分街 → 战前 → 战斗）；
- M7 完成：双端可进房、4 格同步；
- M10 完成：主机权威移动同步跑通；
- M14 完成：热更链路演示通过。

---

## 七、风险与学习点

- **最大改造点**：现有战斗与单机输入紧耦合（`PlayerController` 直接订阅 InputSystem、Animator 驱动状态机）——M9 输入抽象要尽早做，否则同步阶段返工。
- **TCP 手搓要点**：半包/粘包（长度前缀帧）、断线检测（心跳）、收发包线程与主线程交接（队列 + Update 处理）。
- **序列化**：Protobuf.net 消息要预留字段兼容（demo 阶段也要养成加版本习惯）。
- **主机权威**：客户端只上报输入/意图，伤害与血量判定在主机；避免"各端各算一套"。
- **时停/后处理**：现有 `Time.timeScale` 卡肉、`unscaledDeltaTime` 回收等机制要与网络时间分离，先跑通同步再考虑网络下的时停一致性。
- **YooAsset 模式**：先用 EditorSimulateMode 开发，再切 HostPlayMode（本地文件夹）验证热更；注意场景资源也走 AB 后的加载顺序。
- **场景切换**：网络连接放常驻根（DontDestroyOnLoad），场景切换期间会话不中断。

---

## 八、文档维护

- 每完成一个模块，把结论、踩坑、新接口更新到根目录 `开发前须知.md` 与本文档。
- 模块状态可在各表格"验收"列后追加 ✅/进行中标记，保持计划跟随项目走。
