# 战斗会话教学 01：状态同步 vs 帧同步（选型课）

> 面向对象：本项目的开发者（人和 AI agent）。
> 目的：在动手写战斗会话（M9/M10）之前，先想清楚"同步什么、谁说了算、怎么对抗网络抖动"。
> 结论先行：**本项目选主机权威状态同步（快照 + 插值）**，不选帧同步（lockstep）。下面讲清为什么。

---

## 一、先问一个根本问题：联机到底在同步什么？

每个客户端都在本机跑着"自己的世界"。要让两个世界一致，本质上只有两条路：

1. **让所有客户端看到同一份"结果"** —— 服务器把权威状态（位置、血量、动画状态）广播下去，客户端负责"演出来"。
2. **让所有客户端算出同一个"过程"** —— 大家拿到一模一样的输入序列和随机数种子，各自本机模拟，理论上结果一致。

前者叫**状态同步**，后者叫**帧同步**（也叫 lockstep / 确定性模拟）。

---

## 二、状态同步（State Synchronization）

### 核心思想

**服务器（或房主）是唯一权威。** 客户端只上报"意图/输入"，服务器把最终结果（位置、朝向、HP、动画状态、伤害事件）以**快照**的形式定时下发给所有客户端。

```
客户端A 输入(摇杆+攻击) ──► 服务器/房主（唯一权威模拟）
客户端B 输入(摇杆+攻击) ──► 服务器/房主
                               │
                               ├──► 快照(位置/朝向/HP/动画状态) ──► 客户端A 渲染
                               └──► 快照 ──────────────────────► 客户端B 渲染
```

### 配套技术（后面代码里都会出现）

| 技术 | 解决的问题 | 一句话说明 |
| --- | --- | --- |
| **主机权威** | 谁说了算 | 伤害、血量、命中判定全部在主机算，客户端不能自己改血量 |
| **输入上报** | 客户端怎么参与 | 客户端只发"我想往哪走、我按了攻击"，不直接改世界 |
| **定频快照** | 带宽 vs 精度 | 比如每秒 10~20 次下发位置快照，而不是每帧都发 |
| **插值（Interpolation）** | 快照间隔的"卡顿" | 远端角色在两帧快照之间做平滑过渡，看起来是连续的 |
| **预测/回滚（Prediction/Reconciliation）** | 本机手感延迟 | 客户端先本地模拟自己（零延迟手感），再用主机快照纠偏 |
| **事件同步** | 快照照顾不到的"瞬间" | 伤害数字、受击硬直、死亡这类一次性事件单独广播 |

### 优点

- **开发简单、天然防作弊**：判断逻辑只写一份（服务器），客户端怎么改都骗不过权威。
- **对网络抖动宽容**：快照晚到 50ms 只是"插值慢一点"，不会导致逻辑崩坏。
- **允许非确定性**：物理、动画 root motion、AI 随机决策都可以随便用，因为只有主机在算。
- **断线恢复容易**：新客户端加入时，主机直接把当前状态发一遍即可（"晚进房"也能接上）。

### 缺点

- **带宽偏高**：位置、朝向等要持续下发（角色数量越多越明显）。
- **手感有延迟**：远程角色天然有"网络延迟 + 插值延迟"（通常 100~200ms 视觉延迟），格斗类对拼会感觉到"打空气"。
- **状态压缩要花心思**：动画状态、浮点精度、插值缓冲都需要设计。

---

## 三、帧同步（Lockstep / Deterministic Simulation）

### 核心思想

**所有客户端模拟同一份逻辑，输入按"帧"广播，保证人人收到的输入序列完全一致。** 服务器只负责收输入、定帧号、转发，不负责算世界。

```
服务器（仲裁者，只转输入不模拟）
   ├── 每帧：收集所有人输入 → 打成"第 N 帧输入包" → 广播
客户端A：用第 N 帧输入包 + 自己的确定性模拟 → 第 N 帧世界
客户端B：用第 N 帧输入包 + 自己的确定性模拟 → 第 N 帧世界（结果应当一样）
```

### 核心要求：确定性（Determinism）

要让"各自模拟结果一致"，必须满足：

- **纯函数模拟**：同输入 + 同状态 ⇒ 同输出。
- **禁用浮点依赖随机**：随机数要用固定种子的伪随机（且所有端序列一致）。
- **禁用非确定性 API**：物理引擎（PhysX 在不同机器/不同帧率下结果不一致）、Unity 的 `Time.deltaTime`、`Random.value`、字典遍历顺序等，全都不能直接用。
- **固定时间步长**：所有端必须用同一个逻辑帧率（如 60Hz），输入按帧对齐。

典型代表：RTS（星际争霸）、格斗游戏（GGPO 回滚）、MOBA 的早期方案。

### 优点

- **带宽极低**：只发"谁按了什么键"，一场游戏可能只占几 Kbps。
- **回放/观战便宜**：存输入序列 = 存整场游戏。
- **表现完全一致**：适合对判定精度要求极高的对抗（格斗、弹幕）。

### 缺点

- **实现难度大**：确定性是"全有或全无"的约束，Unity 的物理和动画天然不满足，要自己重写/绕开一大片。
- **对抖动敏感**：某帧输入晚到，所有人都要等（lockstep 名字的由来），通常要配合延迟补偿/回滚（GGPO）才不卡。
- **玩家不能随时加入**：新玩家要先把之前所有输入帧下载并回放一遍（demo 阶段基本放弃）。
- **调试痛苦**：一端的小 bug 会导致所有端分叉，且很难复现。

---

## 四、为什么本项目选状态同步（结论）

对照项目现状逐条看：

| 考察点 | 本项目现状 | 状态同步 | 帧同步 |
| --- | --- | --- | --- |
| 玩法类型 | 动作 RPG（根运动动画、锁定、连招、打击感） | 合适 | 困难 |
| 表现引擎 | Animator root motion + PhysX + 行为树随机决策 | 主机独占模拟，无约束 | 全部要改造为确定性 |
| 帧率依赖 | 状态机读 `Time.deltaTime`、动画事件驱动 | 无要求 | 必须固定步长+对齐 |
| 网络层 | TCP（有序、可靠、有重传延迟） | 天生适配（快照丢了等下一帧即可） | 不适合（定帧语义需要低延迟，通常用 UDP） |
| 人数规模 | 2~4 人打 Boss | 快照带宽完全够用 | 省带宽的优势用不上 |
| 开发周期 | 秋招 demo，先跑通再打磨 | 两周内可闭环 | 要把整个战斗系统推倒重来 |
| 防作弊/权威性 | 之后要演示"主机权威" | 天然满足 | 需要额外仲裁层 |

一句话总结：

> **状态同步是"服务器算好了发结果"，帧同步是"所有人算同一道题比答案"。**
> 本项目是动作 RPG、人数少、用 TCP、动画/物理/AI 都重度依赖非确定性引擎，状态同步的缺点（带宽）在这里几乎不构成问题，而帧同步的缺点（确定性改造）却是伤筋动骨的工程。所以：**主机权威状态同步 + 快照 + 插值**。

这也与 `联机与热更开发计划.md` 里"同步模型：主机权威状态同步（快照+插值，不做 lockstep）"的既定决策一致，本节只是把"为什么"补全。

---

## 五、本课要落地的范围（M9 + M10 第一阶段）

1. **输入抽象（M9）**：`IInputProvider`（本地输入 / 远端输入队列），改造 `PlayerController` 与状态机为"轮询式读取"，单机行为不变，遥控可驱动角色。
2. **战斗会话（M10 核心）**：
   - 房主在 Main 场景内启动 `BattleServer`（Listen Server，端口 7778）；
   - 客户端加入 → 握手换名 → 主机回发成员表；
   - 客户端定频上报输入（边沿事件即时、移动状态定频）；
   - 主机以 20Hz 下发状态快照（位置/朝向/动画状态/HP）；
   - 客户端对自己做本地预测 + 主机快照纠偏，对远端做插值幽灵；
   - 伤害/死亡用事件同步，保证各端反馈一致。
3. **怪物同步（M11）与结算（M12）**：留到下一课，本课先保证"双端角色动作一致、伤害一致"。

---

## 六、关键教学点预告（代码里会对应看到）

- **"边沿事件即时发、连续状态定频发"**：攻击/闪避是瞬间意图，立刻上报；摇杆值一直在变，20Hz 定时上报即可省带宽。
- **"快照是结果不是指令"**：客户端拿到快照只用于纠偏/插值，永远不把快照当输入再喂回逻辑。
- **"HP 由事件定、由快照兜底"**：伤害事件负责打击感与即时反馈，快照里的 HP 负责最终一致（幂等覆盖，不做累加）。
- **"预测让本地零延迟，纠偏阈值防瞬移"**：本地先跑，误差超过阈值才往主机位置拉，避免每次快照都抖。
