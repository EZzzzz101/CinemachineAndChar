# 场景优化记录(HANDOFF)

> 给下一个对话看。项目:ZZZ 风格动作游戏,角色靠 **Animator root motion** 移动(`PlayerController.OnAnimatorMove`),**无跳跃**。

## 本次改动的文件

1. **`Assets/场景资源/SixthStreet4.6.fbx.meta`**
   - `addColliders: 0 → 1`:街道导入时生成 MeshCollider(整条街可碰撞)。不想要就改回 `0`。
   - ⚠️ 这个 `场景资源/` 文件夹在 git 里是**未跟踪**状态。

2. **`Assets/Scripts/Character/PlayerController.cs`**
   - 新增字段 `_controller`(CharacterController)、`_groundSnapSpeed`(默认 2)
   - `OnAnimatorMove()` 改为 `_controller.Move(delta + Vector3.down * _groundSnapSpeed * Time.deltaTime)`
     - 保留每帧 `Animator.deltaPosition` 累计(用户要求,动画带位移)
     - 无跳跃,靠轻微向下按压力贴地
     - **兜底**:角色身上没有 CharacterController 时退回原 `transform.position += delta`,不会崩
   - 也就是说:加了 CC 组件才走新逻辑,不加完全不影响原行为

3. **`Assets/Editor/SceneGroundSetup.cs`**(新增,编辑器脚本)
   - 菜单:`Tools/场景/一键地面层+碰撞`
   - 作用:给 Player 加 CharacterController(高1.8/半径0.4/center y0.9/step0.3)、删 Rigidbody、**不碰 applyRootMotion**;把街道挂 Ground 层;删失效的 Plane (1)
   - ⚠️ **跑的时候报错** `Step Offset must be less or equal to <scaled Height> + <scaled Radius> * 2`,脚本中断,后续步骤(挂层/删 Plane)可能没执行
   - 用户说「这个脚本不用也行」—— 可留可删

## 背景决策(重要)

- **碰撞体来源**:用户选「导入生成碰撞体」(addColliders)。回答过:Unity 导入 FBX 默认只带显示、**不带碰撞**,需开 Generate Colliders;碰撞体也可建模师做或引擎内手动加 Box。
- **角色用 CharacterController 而非裸胶囊 Collider**:CC = 胶囊碰撞体 + 移动解算(Move 撞墙/stepOffset 上台阶/slopeLimit/isGrounded)一体,不受物理力影响,匹配 root motion;裸胶囊自己不会挡任何东西,拦不住 `transform.position +=` 的位移。
- **Ground 层**:TagManager **第 3 层本来就是 "Ground"**,但场景里没有物体挂这层。
- **applyRootMotion = 0 是有意的**:实现了 `OnAnimatorMove`,Unity 识别为 By Script,位移正常,不要改。

## 渲染优化(转视角顿帧)

- 症状:转视角顿帧,因为街道 485MB、太大
- 已做:街道根物体勾 **Static** → `Window → Rendering → Occlusion Culling → Bake`(Occlusion data size 2.9MB)
- **结果:转视角不卡了** ✅
- 原因:街道是**很多个子网格**,遮挡剔除能**逐块藏** + Static 让**静态合批**生效;若是一整块大网格则无法分块剔除,遮挡救不了
- 对比渲染量:Game 视图右上角 **Stats** 看每帧 Tris;全程曲线对比用 **Profiler → Rendering** 模块

## 当前状态 / 遗留

- ✅ 转视角顿帧已解决(静态 + 遮挡烘焙)
- ⚠️ 街道 MeshCollider 还在(addColliders:1),物理偏重但用户暂时不关心;要碰撞就留,不要改回 0
- ⚠️ 角色身上 CharacterController **是否已加上不确定**(脚本中途报错);PlayerController 有兜底,不加也能跑
- ⚠️ 场景里一个**敌人**(prefab guid `f5255da99f6af944b907e552b710ca49`)身上挂着一个**默认参数 CC**(高2/半径0.5/step0.3),疑似 Step Offset 报错源头,下次会话可排查
- `安比.prefab` / `Main.unity` 里的大量改动是**用户自己之前的未提交修改**,不是本会话改动

## 遮挡烘焙参数备忘

- 默认:Smallest Occluder 5 / Smallest Hole 0.25 / Smallest Object 0.5
- 剔除不够 → 调小 Occluder / Object;烘焙太慢 → 调大;转视角闪烁 → 调大 Smallest Object
