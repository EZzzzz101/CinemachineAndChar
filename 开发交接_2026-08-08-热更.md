# 明日开工交接（2026-08-08 · 热更）

> 给明天新对话的开发者 / AI agent 看。
> **先读项目根目录两份文档**：`开发前须知.md`（模块地图+约定）、`联机与热更开发计划.md`（分阶段 plan），再读本文件。

---

## 一、项目一句话

Unity 2022.3 URP 动作游戏 demo（秋招用）。**三个场景已全部跑通**：Boot（启动+常驻）→ 六分街 → Main（战斗）。下一步：**热更**——把资源从 Resources 迁移到 AB（YooAsset），对应计划里的 **M13 → M14 → M15**。

---

## 二、当前完成状态（2026-08-08）

### 场景与启动链路
- **Boot**：启动 + 常驻根。`GameEntry`（DontDestroyOnLoad）下挂常驻 Canvas + EventSystem；BGM 常驻。
- **六分街**：`PlayerSpawnPoint` 运行时生成 安比 + 邦布，VCam Follow/LookAt 绑玩家胸口锚点。
- **Main（战斗）**：`PlayerSpawnPoint` 生成安比（`spawnBangboo` 关闭）、`MonsterSpawnPoint` 生成怪兽；`BattleUILoader` 进场景自动加载 GamePanel + 预加载 WinView；LockOnUI 场景预置。
- **启动链路**：`GameEntry.Awake → GameModules.Init()`（Resource → GameState → UI → Camera → SceneLoader → BgmManager → BattleUILoader）→ `BootFlow` 读条 → `GameLaunchView` → 场景。

### 资源加载现状（全部走 Resources，下一步迁移点）
| 调用点 | 路径 |
| --- | --- |
| `UIManager.Open<T>()` | `Resources/UI/Panels/{类型名}`（GameLaunchView / TeamUpView / AddView / WinView / GamePanel / LoadingPanel*） |
| `SceneLoader`（加载面板） | `Resources/UI/Panels/LoadingPanelSixthStreet`、`LoadingPanelMain` |
| `PlayerSpawnPoint` | `Resources/Prefabs/安比`、`Prefabs/Bangboo` |
| `MonsterSpawnPoint` | `Resources/Prefabs/怪兽` |
| `BattleUILoader`（WinView 预加载） | `Resources/UI/Panels/WinView` |
| `BgmManager` | `Resources/BGM/World`、`BGM/Battle` |
| `TeamUpView`（一号位立绘兜底） | `Resources/UI/Portraits/Player` |
| `ResourceManager` | 通用 `Resources.LoadAsync` + 缓存 |

### 相机 / 锁敌（坑最多，已定稿，别再改回克隆方案）
- **索敌相机 = 独立专用 VCam**（`LockOnManager.CreateLockOnCam` 新建，不克隆自由相机）：子管线 `cm` 挂 FramingTransposer（None 模式）+ Composer + Collider。
- `Lock()` 显式给 `Follow/LookAt` + **固定机位**（玩家身后、面向敌人、距离 `_lockCamDistance=3`、抬高 2m），不沿用自由相机朝向。
- **玩家胸口锚点自动创建**：`GetPlayerAimPoint()` 找不到 `CameraBasePoint` 就在玩家根下建一个（`localPosition (0,1.2,0)`）。
- **聚焦点（取景组）**：固定权重 0.7 玩家胸口 + 0.3 敌人 `CameraFocusTransform`（根 + `cameraFocusOffset`，独立于锁定点、可调）。
- **怪兽 LockOnTarget**：锁定点关键字 `Chest/Thorax/Spine`（胸骨）；`cameraFocusOffset` 相机聚焦点独立字段（已调好 y=0.5）。
- **`cm` 是 ThirdPersonCamera 的 Body/Aim 管线子物体，千万别删**（之前差点误删）。
- 玩家生成后必须 `LockOnManager.BindPlayer(player)`（安比 tag 是 Untagged，不能靠 tag）。

### 其他已定稿
- 常驻 UI 根：Boot Canvas 挂 `PersistentUIRoot` 注册给 UIManager；没有注册时 UIManager 自动创建兜底 UICanvas。
- `PlayerInputGate.EnterUI()/ExitUI()`：UI（组队/胜利）打开时禁 Player 动作表 + 显鼠标。
- 聊天功能已砍：ChatInputUI 相关（Main 场景 `ChatInputCanvas`）不用了，可删。
- Main 场景残留可删清单：`GameEntry`（Boot 常驻有重复）、`EventSystem`（重复）、`ChatInputCanvas`、`===Manager===` 下的 CameraManager/TargetManager/AudioManager 副本、空壳 `Player`/`Boss`。

---

## 三、明天任务：热更（M13 → M14 → M15）

### M13 AB 分包 + 构建管线
- 装 YooAsset 包（`Assets/Plugins/UniTask/Runtime/External/YooAsset/` 已有 UniTask 适配脚本，装上即可用）。
- 分包建议：**场景 / 角色 / UI / 战斗**（对齐计划）；写 Editor 构建工具。
- 注意：场景资源走 AB 后，`SceneLoader` 要换 YooAsset 的场景加载接口；出生点/面板/相机引用的 prefab 依赖要进对应包。

### M14 YooAsset 集成（热更链路）
- 先用 `EditorSimulateMode` 开发，再切 `HostPlayMode`（下载源指向**本地文件夹**模拟 CDN）。
- 版本检查 → 下载 → 更新；`BootFlow` 从"模拟读条"改成真实流程：版本检查 → 下载 AB → 预加载关键资源 → **真实进度喂 `LoadingFlow.SetProgress`**。

### M15 IAssetProvider 切换
- `ResourceManager` 抽 `IAssetProvider` 接口：先 Resources 实现（现有），后加 YooAsset 实现。
- 把第二节表格里**所有 Resources.Load 调用点**迁移到 provider（UI 面板、加载面板、出生点、BGM、立绘）。

---

## 四、关键约定与坑（新对话必看）

1. **编码**：脚本统一 UTF-8 无 BOM、中文注释；PowerShell 读文件显式 `-Encoding UTF8`。
2. **csproj 别手改**：Unity 生成；新 .cs 等 Unity 刷新自动收录（验证用 `dotnet build Assembly-CSharp.csproj`，沙箱下需提权）。
3. **输入**：只用新 Input System；UI 事件模块必须 `InputSystemUIInputModule`；代码监听用 `Keyboard.current`。
4. **常驻 vs 场景**：GameEntry/管理器/UI 根/EventSystem/BGM 常驻（Boot）；场景只放出生点/相机/环境；带 Inspector 配置的管理器放 Boot，否则自动创建丢配置。
5. **安比 tag = Untagged**：不要依赖 tag 找玩家，显式注入（出生点 → `BindPlayer` / `PlayerSpawned` 事件）。
6. **对象池**：战斗特效 `VFXPool`；UI 高频 `UIObjectPool`；复用前 DOKill/重置。
7. **事件**：跨模块通信走 `EventBus` + `GameEvents`；UI 只订阅不反向调用。
8. **编辑器操作**：场景/预制体结构在编辑器做；手改 .unity/.prefab YAML 可能被编辑器保存覆盖（本文档项目根目录文件除外）。
9. **相机/锁敌**：见第二节，锁敌相机是独立 VCam + 固定机位 + 显式 Follow/LookAt；别改回"克隆自由相机"。
10. **热更注意**：AB 后 `Resources.Load` 全部失效，必须走 provider；场景加载顺序（版本检查 → 下载 → 预加载）要能支撑 `LoadingFlow` 真实进度。

---

## 五、开工前先确认

- YooAsset 装哪个版本（包管理器），UniTask.YooAsset 适配是否对上；
- AB 分包命名与构建输出目录；
- 本地模拟 CDN 的文件夹路径（HostPlayMode 下载源）；
- 首包策略：内置（Bundles 随包）还是全远程；
- BootFlow 改造点：模拟读条替换成真实流程后的进度来源（下载量 / 预加载数量）。

---

## 六、昨天+今天的问题总结（面试用）

- 出生点模式相机乱飞根因：锁敌相机克隆自由相机时 Follow 未绑定（快照时序）→ 改为独立 VCam + 显式绑定后解决；
- 相机钻地板/飞天上根因：`Lock()` 沿用自由相机朝向 × 距离 20 → 固定机位解决；
- 玩家出画根因：跟角色根（脚底）→ 自动创建胸口锚点解决；
- Main 场景重复对象（GameEntry/EventSystem）清理；
- 锁定点与相机聚焦点分离（`lockOnPointOffset` vs `cameraFocusOffset`）。
