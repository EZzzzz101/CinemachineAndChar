# 开发前须知（Developers & Agents README）

> 本文件供**人类开发者和 AI agent** 在动手改代码前阅读：项目模块地图、基础库用法、开发约定、常见坑。
> 阅读顺序建议：第 0 节 TL;DR → 目录结构 → 相关模块章节 → 开发约定。

---

## 0. 快速上手（TL;DR）

- **引擎/管线**：Unity 2022.3 + URP 14（后处理通过全局 Volume 驱动），Input System（New Input），TMP，Cinemachine，DOTween，UniTask。
- **启动流程**：场景里的 `GameEntry` → `GameModules.Init()` 按依赖顺序初始化 `ResourceManager → GameStateManager → UIManager`；各 Manager 继承 `GameModule<T>`（自带单例 + 一次性初始化）。
- **跨模块通信**：一律走 `EventBus` + `GameEvents` 常量，不要跨类直接 `GetComponent` 互相调用（UI ↔ 战斗解耦）。
- **战斗命中**：玩家/怪物共用 `AttackHitHelper`（检测命中 + 暴击 + 发 `DamageData`），受击反馈由**被击者自己**播；伤害跳字由 `GamePanel` 订阅 `HitLanded` 生成。
- **特效/高频对象**：战斗特效用 `VFXPool`（自动回收）；UI 频繁对象用 `UIObjectPool<T>`；计时用 `TimerManager`（真实时间，不受 timeScale 影响）。
- **agent 注意**：脚本是 **UTF-8 无 BOM**、中文注释；Unity 常驻运行时会自动导入新脚本/生成 `.meta` 和 csproj（**不要手改生成的 csproj**）；改场景文件前注意 Unity 可能正在保存覆盖。
- **临时/测试脚本**：`Assets/Scripts` 根目录的 `InputTest.cs`、`DotweenTest.cs`、`NewBehaviourScript.cs`、`AStarGrid.cs`，以及 `AI/BehaviorTree/Test.cs` 都是测试/占位代码，不是正式模块，改动前先确认是否仍被使用。

---

## 1. 目录结构（Assets/Scripts）

| 目录 | 职责 |
| --- | --- |
| `Core/` | 启动与基类：`GameEntry`（入口）、`GameModule<T>`（模块单例基类）、`GameModules`（统一初始化）、`EventBus`、`GameEvents`、`IDamageable` |
| `Data/` | 纯数据结构：`HPData`、`DamageData`（跨层共享的 struct 放这里） |
| `Managers/` + `Manager/` | 全局管理器：`ResourceManager`（Resources 异步加载+缓存）、`GameStateManager`、`SceneLoader`、`UIManager`、`LockOnManager`、`CameraManager`、`CameraShake`、`HitPauseManager`、`DashMotionBlur`、`TargetManager`、`UnitAudio` |
| `Character/` | 玩家：`PlayerController`、`CharacterVFX`、`CharacterAnimationEvents`；`Core/` 状态机（Locomotion + Combo/Action）；`Data/` 配置 SO（`CharacterDataSO`、`CharacterAudioSO`、`CharacterBaseAttribute`）；`Pet/` 邦布 + LLM 子系统 |
| `AI/BehaviorTree/` | 怪物行为树：`BehaviorTreeRunner`、`BehaviorTreeSO`、`BTBlackboardSO`、`Blackboard`、`BossBrain`、`BossMotor`、`MonsterAttackConfigSO`、全部节点（Actions/Composites/Conditions/Decorators） |
| `UI/` | `GamePanel`（主 HUD）、`HPBar`、`LockOnUI`；`Core/`：`UIManager`、`UIView`、`IUIView`、`UIObjectPool`、`DamageText` |
| `Enemy/` + `Target/` | `LockOnTarget`（锁定点/预警点）、`Targetable` |
| `Tool/` | `Utils/Singleton`、`Utils/VFXPool`、`Utils/PooledVFX`、`TimerManager/GameTimer`、`TimerManager/TimerManager` |
| `Cam/` | 相机辅助（隐藏光标、滚轮缩放等） |
| `Assets/Editor/` | 编辑器工具：`BehaviorTree/*`（行为树可视化编辑器）、`SceneGroundSetup`、`CameraColliderSetup` |

---

## 2. 游戏总控与模块初始化

### GameEntry（启动）
`Assets/Scripts/Core/GameEntry.cs` — 挂在启动场景 GameObject 上，`Awake` 里 `DontDestroyOnLoad` + `GameModules.Init()`。

### GameModule<T>（模块基类）
- 继承即获得：单例 `Instance`（找不到自动创建）、`DontDestroyOnLoad`、一次性初始化 `Init()`（按 Type 哈希去重）。
- 子类实现 `OnInit()` 写真正的初始化逻辑。
- 新 Manager 的固定套路：`public class XxxManager : GameModule<XxxManager> { protected override void OnInit() { ... } }`，然后在 `GameModules.Init()` 按依赖顺序加一行。

### GameModules（初始化顺序）
`ResourceManager`（基础资源）→ `GameStateManager` → `UIManager`（依赖资源加载面板）。`Shutdown()` 会 `EventBus.Clear()`。

### 另一种单例：Singleton<T>
`Tool/Utils/Singleton`（namespace `SingletonTool`）— 自动创建、不重复，与 GameModule 类似但**没有统一 Init 流程**。用于无需初始化顺序的服务：`TimerManager`、`CameraShake`、`HitPauseManager`、`DashMotionBlur`、`LockOnManager`、`CharacterVFX`。

---

## 3. 事件中心（EventBus / GameEvents）

- `EventBus.Subscribe<T>(eventName, callback)` / `Unsubscribe<T>` / `Emit<T>(eventName, data)`，另有无参版本。
- 所有跨模块事件名集中定义在 `GameEvents`（字符串常量，如 `hp.changed`、`hit.landed`、`player.died`、`enemy.died`、`game.state.changed`）。
- **约定**：UI 只订阅事件、不反向调用战斗类；战斗侧只 Emit、不依赖 UI。
- 常见事件：`HitLanded`（伤害数字，`DamageData`）、`HPChanged`/`HPTextChanged`（血条/血字，`HPData`）、`PlayerDied`（失败结算接口）、`EnemyDied`（胜利结算接口）、`GameStateChanged`、`EnemySpawned`、`Dungeon*`、`PlayerConnected/Disconnected`。
- 订阅/退订在 `OnEnable/OnDisable` 成对出现（见 `GamePanel`），防止泄漏。

---

## 4. 对象池与高频对象管理

### VFXPool（战斗特效，新增）
`Tool/Utils/VFXPool.cs` + `PooledVFX.cs` — 静态对象池，按 prefab 分池。
- `VFXPool.Spawn(prefab, pos, rot, parent = null, lifetime = 2f)`：取池实例（自动挂 `PooledVFX`）；`lifetime > 0` 到点自动回收（真实秒），`<= 0` 由调用方显式 `Despawn`。
- `VFXPool.Despawn(instance)` / `Prewarm(prefab, count)` / `Clear()`。
- 已接入：玩家受击、怪物受击、玩家攻击挥砍（`CharacterVFX`）、怪物攻击预警（`BTAttack` 显式 Despawn）。
- **注意**：池实例复用前要重置状态（参考 `DamageText` 的 DOKill/颜色/缩放复位）；特效 prefab 若自带自销毁脚本会与池冲突。

### UIObjectPool<T>（UI 频繁对象）
`UI/Core/UIObjectPool.cs` — 通用 UI 组件池，`Get()`/`Return(obj)`，预分配 + 上限；伤害数字用它。

### TimerManager 计时器池
`GameTimer`（真实时间，`unscaledDeltaTime`）池化复用；`GetRealTimer(duration, cb)` 无返回值、`GetTimer(...)` 返回句柄可 `Cancel`。

---

## 5. 时间缩放模块

- `HitPauseManager.Trigger(duration, timeScale = 0.05f)`：全局 `Time.timeScale` 卡顿，用 `TimerManager.GetRealTimer` 恢复（真实秒，不受 timeScale 影响）。
- 完美闪避：`PlayerController.TakeDamage` 无敌分支里 `HitPauseManager + CameraShake + DashMotionBlur`（URP Motion Blur，强度包络曲线，`unscaledDeltaTime` 推进）。
- **约定**：所有"真实时间"逻辑（计时、淡出、池回收）用 `Time.unscaledDeltaTime`；所有"游戏时间"逻辑用 `Time.deltaTime`。时停期间不要依赖 scaled 计时。

---

## 6. 资源加载

`ResourceManager`：`LoadAsync<T>(path)`（Resources.LoadAsync + 缓存）、`InstantiateAsync<T>(path, ...)`（UniTask 异步）。
目前只有面板懒加载（`UIManager.Open<T>` 从 `Resources/UI/Panels/{类型名}` 取）会用到；正式资源流程可在此基础上扩展。

---

## 7. UI 系统

- 面板继承 `UIView`（自动注册到 `UIManager`），可放场景里（如 `GamePanel`）或放 `Resources/UI/Panels/` 懒加载。
- `UIManager`：`Open<T>()` / `Close<T>()` / `Get<T>()`；持有根 Canvas/Transform。
- 主 HUD `GamePanel`：订阅 HP/伤害/结算事件；**伤害数字** = 订阅 `HitLanded` → `VFXPool` 之外用 `UIObjectPool<DamageText>` 生成，世界命中点经 `Camera.main.WorldToScreenPoint → ScreenPointToLocalPointInRectangle` 定位；暴击 = 图标+数字组合。
- `DamageText`：对象池友好（Show 前 DOKill/复位，动画结束回调回收）；`normalColor/critColor`、`critIconGap` 在 Inspector 配。
- 结算界面（胜利/失败）预留接口：订阅 `EnemyDied` / `PlayerDied`。

---

## 8. 角色系统

### 玩家 PlayerController
- 持有：`Animator`、`MoveInputMY`、`PlayerInput`、`CharacterController`、`PlayerAudio`、两个状态机（`Locomotion` + `Action`）、基础属性 `baseAttribute`。
- 实现 `IDamageable.TakeDamage`：扣血 → 完美闪避判定（无敌帧）→ 发 HP 事件 → 受击反馈（特效/音效/震屏/顿帧/硬直）→ 血量归零只发一次 `PlayerDied`（暂不锁行为）。

### 基础属性 CharacterBaseAttribute
`Character/Data/CharacterBaseAttribute.cs` — 可序列化：`attack` / `maxHP` / `critRate` / `critDamage`；玩家在 Inspector 配一份。`attack` 目前是预留（伤害仍由 `ComboStepData.damage` 决定）。

### 状态机（双 FSM）
- 基类 `StateMachine`：`ChangeState(IState)` / `Update()` / 动画事件转发。
- **Locomotion**（移动）：`Idle / Run / Sprint / Dash / TurnBack`，输入驱动，Animator `HasInput` Bool。
- **Action**（动作）：`ActionNullState / ATKingState(连招) / HitState(受击硬直) / DeathState(暂未启用)`；连招数据 `ComboResuableData` 由 `ActionStateMachine` 持有。
- **动画 ↔ 状态机桥接三件套**（都在 `Character/`）：
  - `AnimationEnterBehaviour`：状态进入事件 → `PlayerController.OnAnimationTranslateEvent`（枚举：DashFront/DashBack/Atk/TurnBack）；
  - `AnimationExitBehaviour`：状态退出事件 → `OnAnimationExitEvent`；
  - `CharacterAnimationEvents`：动画剪辑关键帧事件（`ATK`/`EnablePreInput`/`CancelAttackColdTime`/`DisableLinkCombo`/`EnableMoveInterrupt`）→ 转发到 Combo 状态。
- **约定**：动画状态名/参数名与代码里的字符串保持一致（如 `"BeHit"`、`"Death"`、`"HasInput"`）；新增状态 = 建类继承 `PlayerComboState`/`LocomotionState` + 在状态机构造函数注册。

### 连招配置
`ComboConfigSO`（右键 Create → Combo/ComboConfig）+ `ComboStepData`：每段动画名、输入窗口、伤害、音效、震屏/顿帧、特效；玩家 prefab 的 `comboConfigSO` 引用。

---

## 9. 怪物与行为树

### 架构
- **数据**：`BehaviorTreeSO`（扁平节点列表 + 连线 + 根节点 ID，节点参数 JSON 化）；`BTBlackboardSO`（黑板键 schema）。
- **运行时**：`BehaviorTreeRunner.BuildTree()` 反射建树，按 `_tickInterval`（默认 0.1s）Tick；`Blackboard` 自动绑定 `_owner/_transform/_animator/_audio`，键值读写 `Get/Set`。
- **编辑器**：`Assets/Editor/BehaviorTree/` 可视化窗口，**双击 .asset 打开**；节点类型由 `BTNodeFactory` 反射扫描所有带 `[BTNode("中文名","分类","描述")]` 的类。
- **动画桥接**：`BTAnimationExitNotifier`（StateMachineBehaviour）按状态 hash 把"动画进出"写黑板（活跃集合 + 退出信号），节点据此等动画播完。

### 节点开发规范
- 动作节点：继承 `BTAction<T>`，参数用 `[Serializable] struct T`（JsonData 序列化），实现 `OnEnter/OnExecute/OnExit`，加 `[BTNode(...)]` 属性即可进编辑器。
- 条件节点：继承 `BTCondition<T>`（`BTBlackboardCondition` 已能做数值比较）。
- 组合/装饰：`BTSequence`、`BTSelector`、`BTReactiveSelector`、`BTWeightedRandom`、`BTSubTree`、`BTCooldown`、`BTInverter`、`BTRepeater`、`BTSucceeder`。
- **ReactiveSelector 语义**：每 tick 从 0 全量重评，高优先级分支 Running/Success 时递归 Abort 低优先级子树；"一帧完成"的让位节点（如 `BTHitReaction`）OnExit 不能有全局副作用（会每 tick 触发）。

### 怪物实体
- `BossBrain`：血量/阶段/`IsDead`，实现 `IDamageable`；每帧写 `_hpRatio`、`_timeSincePlayerAttack` 到黑板。
- `BossMotor`：行为树定决策、Motor 定执行（每帧读 `_faceTarget`/`_standoff` 平滑转向/移动）；死亡后 `IsDead` 守卫停转。
- `MonsterAttackConfigSO` + `MonsterAttackStepData`：每段攻击的伤害/时机/音效/特效/霸体。
- **死亡流程**：行为树根挂"死亡分支"（`_hpRatio<=0` → `BTDeath` 节点，最高优先级）；`BTDeath` 触发 `Death` Trigger、清移动/旋转标志、黑板 `_dead` 防重入；`BossBrain` 发 `EnemyDied`。Animator 侧需配：`Death` Trigger 参数 + `Death` 状态 + AnyState→Death 过渡（带条件）+ 死亡状态挂 `BTAnimationExitNotifier`。

---

## 10. 战斗与伤害

- `AttackHitHelper`：玩家/怪物**共用**的命中结算入口。
  - `DetectHits`：OverlapSphere + 前方锥形 + 自排除，收集 `IDamageable`。
  - `DealDamage`：命中 → 暴击判定（可选参数 `CharacterBaseAttribute`，不传/`critRate<=0` 不暴击）→ `TakeDamage(最终伤害)` → `Emit(HitLanded, DamageData(damage, isCritical, hitPoint))`。
- `IDamageable`：`TakeDamage(float, GameObject attacker)`；受击反馈由被击者自己决定（闪避时不播）。
- 伤害数字：`GamePanel` 订阅 `HitLanded`，在命中点生成跳字（见 UI 章节）。
- **约定**：伤害计算收敛在 `AttackHitHelper`，新增"减伤/格挡/弹刀"改被击方即可，不要在各攻击节点里各写一套。

---

## 11. 相机与锁定

- `LockOnManager`（Cinemachine 双 VCam）：自由相机 ↔ 索敌相机（Composer + TargetGroup），中键切换；`LockOnTarget` 提供锁定点/预警点骨骼，`LockOnUI` 显示锁定框。
- `CameraManager`：移动方向相对相机换算。
- `CameraShake`：`CinemachineImpulseSource.GenerateImpulseWithForce(force)`（IgnoreTimeScale）。
- `CameraHideCursor` / `CameraZoom`：辅助。

---

## 12. 其他子系统

- **Pet/LLM**（`Character/Pet/`）：邦布 `BangbooBrain`、`LLMClient/PromptBuilder/ResponseParser`、`CharacterProfileSO`、聊天 UI —— 独立子系统，有各自的编辑器扩展。
- **场景工具**：`SceneLoader`（异步切场景 + Loading UI）、`Editor/SceneGroundSetup`（一键地面层+碰撞）、`Editor/CameraColliderSetup`。
- **音频**：`UnitAudio`、`MonsterAudio`、`PlayerAudio`；`CharacterAudioSO` 集中管理角色音效。

---

## 13. 开发约定与注意事项

1. **编码**：脚本统一 UTF-8 无 BOM、中文注释；用 PowerShell/工具读文件时显式按 UTF-8 读，否则中文乱码（Windows 默认按 GBK 解码）。
2. **生成的 csproj 别手改**：`Assembly-CSharp.csproj` 等由 Unity 生成；新增 .cs 文件后等 Unity 刷新会自动包含。命令行编译验证可用 `dotnet build Assembly-CSharp.csproj`（需把新文件临时加进 csproj 或等 Unity 刷新）。
3. **中文路径/文件名**：`Actions(动作节点)`、`安比.prefab`、`特效/` 等含中文，命令行脚本里注意引号与编码。
4. **Unity 常驻运行**：改场景/预制体 YAML 时可能被编辑器保存覆盖；场景里新建对象尽量让用户在编辑器里做，代码只做运行时逻辑。
5. **对象池复用**：拿回实例先重置（DOTween 要 `DOKill`、颜色/缩放/透明度复位），生命周期交回池（回调回收），否则会出现"第二次显示透明/缩放残留/位置飘"。
6. **时停**：`timeScale` 会全局变慢；计时/回收/后处理淡出用真实时间，UI 动画如需要可 `SetUpdate(true)`。
7. **已知编辑器 bug**：Animator 窗口偶发 `GenerateConnectionKey` NRE（断过渡/缓存问题），重启/关窗可解，不是代码问题。
8. **git**：项目改动由开发者自行提交；agent 交付后保持工作区整洁，不主动 commit。

---

## 14. 常见开发流程速查

- **加一个战斗特效**：预制体做好 → `VFXPool.Spawn(prefab, pos, rot, parent, lifetime)`（需要显隐控制就传 `0f` 并自己 `Despawn`）→ 需要预热就在 Awake 里 `Prewarm`。
- **加一个行为树节点**：建类继承 `BTAction<T>`/`BTCondition<T>` → 定义 `[Serializable] struct T` → 加 `[BTNode("中文名","Action/xxx","描述")]` → 编辑器里双击树资产添加并配参 → 运行时 `BehaviorTreeRunner` 反射自动识别。
- **加一个 UI 面板**：继承 `UIView` → 放场景或 `Resources/UI/Panels/{类名}` → 在 `OnEnable/OnDisable` 订阅/退订事件。
- **加一个全局管理器**：继承 `GameModule<T>` 实现 `OnInit` → 在 `GameModules.Init()` 按依赖顺序注册。
- **加一个状态**：继承 `PlayerComboState`/`LocomotionState` → 在对应状态机构造函数里 `new` 出来并暴露属性 → 动画事件/代码切过去。
